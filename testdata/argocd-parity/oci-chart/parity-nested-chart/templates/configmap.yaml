apiVersion: v1
kind: ConfigMap
metadata:
  name: parity-oci-helm-nested
  namespace: {{ .Release.Namespace }}
data:
  chartName: {{ .Chart.Name }}
  chartVersion: {{ .Chart.Version }}
  fromValues: {{ .Values.fromValues }}
