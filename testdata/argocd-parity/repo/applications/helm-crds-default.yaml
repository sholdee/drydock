apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: parity-helm-crds-default
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git://argocd-parity-git.argocd-parity.svc.cluster.local/repo.git
    targetRevision: HEAD
    path: charts/crds-default
    helm:
      releaseName: parity-helm-crds-default
  destination:
    name: in-cluster
    namespace: parity-helm-crds-default
