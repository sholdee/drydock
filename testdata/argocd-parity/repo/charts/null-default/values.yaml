# debugVerbose is intentionally null (bare key with no value).
# Helm v4 — bundled by Argo CD's repo-server since v3.5 — strips null chart
# defaults during coalescing, so hasKey returns false and the guarded key is
# absent from the rendered output on both sides.  (Argo CD v3.4's helm v3
# preserved the null: hasKey → true, rendering an empty string.)
debugVerbose:
