version: "2"

run:
  timeout: 5m

linters:
  enable:
    - bodyclose
    - copyloopvar
    - dupword
    - durationcheck
    - errcheck
    - errorlint
    - exhaustive
    - gocritic
    - gocyclo
    - govet
    - ineffassign
    - intrange
    - misspell
    - modernize
    - nakedret
    - nilerr
    - nilnil
    - nolintlint
    - prealloc
    - staticcheck
    - unconvert
    - unused
    - usestdlibvars
    - wastedassign
    - whitespace
  settings:
    gocyclo:
      min-complexity: 15
    nolintlint:
      require-explanation: true
      require-specific: true
    errcheck:
      check-type-assertions: true
  exclusions:
    presets:
      - std-error-handling
    rules:
      - linters:
          - modernize
        text: "omitzero:"

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
