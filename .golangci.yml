version: "2"

run:
  timeout: 5m

linters:
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - canonicalheader
    - copyloopvar
    - dupword
    - durationcheck
    - errcheck
    - errchkjson
    - errorlint
    - exhaustive
    - gocheckcompilerdirectives
    - gocritic
    - gocyclo
    - govet
    - ineffassign
    - intrange
    - misspell
    - modernize
    - makezero
    - nakedret
    - nilerr
    - nilnesserr
    - nilnil
    - nolintlint
    - prealloc
    - reassign
    - staticcheck
    - unconvert
    - unused
    - usetesting
    - usestdlibvars
    - wastedassign
    - whitespace
  settings:
    gocyclo:
      min-complexity: 15
    nolintlint:
      require-explanation: true
      require-specific: true
    errcheck:
      check-type-assertions: true
  exclusions:
    presets:
      - std-error-handling
    rules:
      - linters:
          - modernize
        text: "omitzero:"

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
